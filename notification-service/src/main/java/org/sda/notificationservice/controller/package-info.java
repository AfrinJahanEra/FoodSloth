/**
 * REST controllers. Exposes the endpoints that api-gateway routes to this service.
 *
 * Part of notification-service. This file also keeps the package tracked in git,
 * because git does not record empty directories.
 */
package org.sda.notificationservice.controller;
