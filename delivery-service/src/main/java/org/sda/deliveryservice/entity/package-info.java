/**
 * MongoDB document models. Classes here are annotated with @Document and persisted to food_delivery_db.
 *
 * Part of delivery-service. This file also keeps the package tracked in git,
 * because git does not record empty directories.
 */
package org.sda.deliveryservice.entity;
