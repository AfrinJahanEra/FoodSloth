/**
 * MongoDB document models. Classes here are annotated with @Document and persisted to food_user_db.
 *
 * Part of user-service. This file also keeps the package tracked in git,
 * because git does not record empty directories.
 */
package org.sda.userservice.entity;
