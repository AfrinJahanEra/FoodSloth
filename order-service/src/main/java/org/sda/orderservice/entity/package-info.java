/**
 * MongoDB document models. Classes here are annotated with @Document and persisted to food_order_db.
 *
 * Part of order-service. This file also keeps the package tracked in git,
 * because git does not record empty directories.
 */
package org.sda.orderservice.entity;
